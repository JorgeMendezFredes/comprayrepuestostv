---
interface Props {
    title: string;
    description: string;
    suggestedPrompt?: string;
    sizeHint?: string;
}

const {
    title,
    description,
    suggestedPrompt,
    sizeHint = "1200x800",
} = Astro.props;
---

<div class="image-placeholder">
    <svg
        width="48"
        height="48"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        opacity="0.5"
    >
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
        <circle cx="8.5" cy="8.5" r="1.5"></circle>
        <polyline points="21 15 16 10 5 21"></polyline>
    </svg>
    <p class="image-placeholder-title">📷 {title}</p>
    <p class="image-placeholder-desc">{description}</p>
    {
        suggestedPrompt && (
            <p class="image-placeholder-desc">
                <em>Prompt sugerido: {suggestedPrompt}</em>
            </p>
        )
    }
    <p class="image-placeholder-desc">Tamaño recomendado: {sizeHint}</p>
</div>
